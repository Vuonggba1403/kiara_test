import 'package:flutter/material.dart';

/// Central place to define and reuse app colors
class AppColors {
  // Backgrounds
  static const Color scaffoldDark = Color(0xFF1E293B);
  static const Color scaffoldDeeper = Color(0xFF0F172A);

  // Primary / accent greens
  static const Color primaryGreen = Color(0xFF7CB342);
  static const Color primaryGreenDark = Color(0xFF558B2F);
  static const Color primaryGreenDarker = Color(0xFF33691E);
  static const Color lightGreen = Color(0xFF8BC34A);
  static const Color softerGreen = Color(0xFF66BB6A);

  // Other accent colors
  static const Color blueAccent = Color(0xFF42A5F5);
  static const Color orangeAccent = Color(0xFFFFA726);
  static const Color lightBlueAccent = Color(0xFF29B6F6);
  static const Color stressRed = Color(0xFFEF4444);
  static const Color text2Color = Color(0xFF99A1AF);

  // Text / icon helpers
  static const Color mutedText = Color(0xFF64748B);

  // Card / surface colors
  static const Color cardDark = Color(0xFF1E293B);

  // Gradients base colors
  static const Color wellbeingGradientStart = Color(0xFF2D4A2B);
  static const Color wellbeingGradientEnd = Color(0xFF1E3A1E);
  static const Color textColor = Color(0xFFFFFFFF);
}
